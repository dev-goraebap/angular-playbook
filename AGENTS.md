# 에이전트 작업 지침

본 문서는 이 저장소에서 에이전트가 작업할 때 지켜야 하는 규칙을 정의합니다. 저장소의 구조와 실행 명령은 [README](README.md)에 있으며 여기에 다시 적지 않습니다.

## 1. 원본이 어디에 있는가

**아키텍처 문서의 원본은 이 저장소가 아닙니다.** [refarch-angular-springboot](https://github.com/dev-goraebap/refarch-angular-springboot)이 원본을 소유하고 이 저장소는 그것을 서브모듈로 가져와 발행합니다. 원본을 그쪽에 두는 이유는 문서를 따르는 코드가 같은 저장소에 있어 규칙과 구현이 한 커밋에서 함께 움직이기 때문입니다.

| 대상 | 위치 | 고치는 저장소 |
| :--- | :--- | :--- |
| **아키텍처 문서** | `external/refarch-angular-springboot/docs/architecture/` | refarch-angular-springboot |
| **에세이와 글** | `docs/architectures/index.md` · `docs/posts/` | 이 저장소 |
| **발행 파이프라인과 화면** | `scripts/build-docs.mjs` · `src/` | 이 저장소 |

> **중요: 서브모듈 안의 수정은 이 저장소의 커밋에 담기지 않습니다**
>
> `external/` 아래를 고쳤다면 참조 구현 저장소에서 커밋하고 푸시한 뒤, 이 저장소에서 서브모듈이 가리키는 커밋을 옮겨 함께 커밋합니다. 그 단계를 빠뜨리면 로컬에서는 반영된 것으로 보이지만 배포에는 옛 문서가 올라갑니다.

Angular 문서는 `external/refarch-angular-springboot/docs/architecture/angular/` 아래에 있습니다. 이하 `.../`로 줄입니다.

| 찾는 것 | 원본 |
| :--- | :--- |
| 계층 배치, 임포트 방향 | `.../references/패키지-배치와-참조-규칙.md` |
| 화면 골격, 스크롤, 상태 화면 | `.../references/레이아웃.md` |
| 색·서체·간격 토큰 | `.../references/디자인-시스템과-토큰.md` |
| 반응형과 적응형의 구분 | `.../references/적응형-UI.md` |
| 문서 작성 규칙, 실측으로 확인한 사항 | `.../references/개발-환경.md` |
| 결정과 기각한 대안 | `.../decisions/` |
| 앱 분리와 계약, 배포 단위 | `external/refarch-angular-springboot/docs/architecture/index.md` |

**층은 위에서 아래로 좁혀집니다.** 위 층의 결정이 아래 층의 전제이며 반대 방향은 없습니다. 스택 문서의 규칙을 바꾸기 전에 그것이 상위 층에서 내려온 전제인지 확인합니다.

새 규칙을 만들었다면 코드에만 두지 않고 해당 문서에 적습니다. 반대로 문서에 이미 있는 내용을 코드 주석이나 본 문서에 옮겨 적는 것을 **금지**합니다. 두 벌이 되면 한쪽이 반드시 낡습니다.

## 2. 작업을 끝내기 전에

```bash
npm run check     # 린트 · FSD 검사 · 빌드
```

**이것을 통과하지 못한 상태로 완료를 보고하지 않습니다.** 세 가지를 한 번에 검사하며, FSD 위반은 Steiger가 빌드를 실패시킵니다.

마크다운을 고쳤다면 `npm run docs:build`가 규칙 위반을 잡아 멈춥니다. 서브모듈의 문서도 같은 검사를 받으므로, 참조 구현 저장소에서 문서를 고친 뒤에는 이 저장소에서 이것을 돌려야 링크와 절 참조가 성립하는지 확인됩니다. 실패 메시지에 파일명과 사유가 나오므로 그것을 읽고 고칩니다. 검증에 실패해도 기존 생성물은 보존되므로 개발 서버가 죽지는 않습니다.

`src/shared/markdown/generated/`는 빌드 산출물입니다. **직접 수정하지 않습니다.** 고쳐야 할 것은 마크다운 원본이거나 `scripts/build-docs.mjs`입니다.

## 3. 검증하지 않은 것을 단정하지 않습니다

이 저장소의 문서는 수치와 실측을 근거로 씁니다. 같은 기준을 작업에도 적용합니다.

| 구분 | 준수 지침 (Do) | 금지 지침 (Don't) |
| :--- | :--- | :--- |
| **번들 크기** | 빌드 출력의 값을 인용합니다 | "가벼워집니다"라고 씁니다 |
| **대비·색** | 계산해서 비율을 적습니다 | "충분히 잘 보입니다"라고 씁니다 |
| **동작 확인** | 확인한 경로와 방법을 함께 밝힙니다 | 확인하지 않은 것을 확인한 것처럼 보고합니다 |

브라우저로 눈으로 본 것과 마크업·번들만 검사한 것은 다릅니다. 후자만 했다면 **무엇을 확인하지 못했는지 밝힙니다.**

증상이 보이는 층과 원인이 있는 층이 다른 사례가 반복해서 나왔습니다. CSS나 계산으로 증상을 보정하기 전에 어느 층이 그 값을 결정하는지 먼저 확인합니다. 축적된 사례는 개발 환경 문서 7절에 있습니다.

## 4. 결정을 남기는 곳

설계 선택과 기각한 대안은 `external/refarch-angular-springboot/docs/architecture/angular/decisions/`에 ADR로 남깁니다. 채번 규칙과 대상 선정 기준은 `.../angular/index.md` 9절이 원본입니다. 이 파일들은 참조 구현 저장소 소유이므로 그쪽 저장소에서 커밋합니다.

> **주의: 과거 ADR을 덮어쓰지 않습니다**
>
> 결정이 번복되면 신규 ADR을 발행해 이전 것을 `Superseded by`로 참조합니다. 소규모 완화와 예외 추가는 원 ADR의 개정 절에 날짜와 함께 추기합니다. 옛 기록을 고치면 왜 그런 판단을 했는지가 사라집니다.

ADR 목록은 사이드바가 보여주므로 개요 문서에 등록 표를 두지 않습니다. 새 ADR을 만들면 파일 하나만 추가하면 됩니다.

## 5. 문체

저장소의 모든 마크다운은 `~합니다` 존댓말 서술형으로 씁니다. 기존 문서가 다른 톤이라는 이유로 그 톤을 따라가지 않습니다.

| 항목 | 규격 |
| :--- | :--- |
| **적는 대상** | 구현 방법이 아니라 그 선택을 한 이유와 포기한 것 |
| **표현** | 공학적으로 뜻이 확정되는 용어. 비유와 의인화를 쓰지 않습니다 |
| **규칙 선언** | 허용 · 금지 · 필수로 씁니다. "지양하면 좋습니다"는 규칙이 아니라 의견입니다 |
| **강조 블록** | 한 절에 하나. 놓치면 데이터 손상이나 기동 실패로 이어지는 것만 |
| **이모지** | 제목과 본문 어디에도 쓰지 않습니다 |

커밋 메시지와 코드 주석은 이 규격의 적용 대상이 아닙니다. 각 도구의 관례를 따릅니다.

## 6. 커밋

제목은 평서형 한 줄로 무엇을 했는지 적습니다. 본문에는 **왜 그렇게 했는지와 무엇을 포기했는지**를 적습니다. 기존 이력의 형태를 그대로 따릅니다.

```text
주제 필터를 목록 화면으로 옮기고 하단과 목차를 채운다

주제 화면을 없애고 목록에서 거른다. 화면을 나누면 같은 목록 렌더링이 두 벌이
되고 여러 주제를 겹쳐 고르는 것도 표현할 수 없다. 선택은 URL 이 소유하므로
새로고침과 뒤로가기, 링크 공유가 별도 구현 없이 동작한다.
```

커밋과 푸시는 요청받았을 때만 수행합니다.

## 7. 배포

`main`에 올라간 것은 자동으로 배포되어 [goraebap.xyz](https://goraebap.xyz)에 반영됩니다. **`main`이 곧 운영입니다.** 워크플로는 `.github/workflows/deploy.yml`이며, 전 경로가 정적 생성 대상이므로 `dist/blog/browser`만 올립니다.

`public/CNAME`이 커스텀 도메인을 소유합니다. 저장소 설정이 아니라 이 파일이 원본이므로 도메인을 바꾸려면 여기를 고칩니다.

## 8. 자주 걸리는 것

측정으로 확인한 함정은 개발 환경 문서 7절이 원본입니다. 아래는 그 문서가 다루지 않는 작성 시점의 함정입니다.

**컴포넌트의 인라인 `template`과 `styles` 안에서는 주석에 백틱을 쓰지 않습니다.** 둘 다 템플릿 리터럴이므로 주석 안의 백틱 하나가 문자열을 끊습니다. 오류는 끊긴 자리가 아니라 한참 뒤에서 `',' expected`로 나타나 원인을 찾기 어렵습니다. 코드 식별자를 언급할 때는 백틱 없이 그대로 적습니다.

```ts
// 이렇게 쓰면 템플릿이 끊깁니다
template: `
  <!-- height 의 계산값은 `auto` 로 같습니다 -->
`,

// 이렇게 씁니다
template: `
  <!-- height 의 계산값은 auto 로 같습니다 -->
`,
```

별도 파일로 뺀 스타일(`styleUrl`)과 마크다운에는 이 제약이 없습니다. 이 저장소는 코드 주석에 식별자를 자주 인용하므로 실제로 두 번 걸렸습니다.
