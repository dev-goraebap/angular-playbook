---
slug: decoupled
title: '디커플드 아키텍처'
description: '프론트엔드와 백엔드를 분리하는 구성의 선택 조건과 대가'
order: 0
---

# 디커플드 아키텍처

본 문서는 프레젠테이션과 업무 로직을 별도 애플리케이션으로 나누고 API로만 통신하는 구성을 언제 고르고 그 대가로 무엇을 치르는지를 정의합니다.

이 폴더의 문서는 전부 **이 구성을 골랐다는 전제** 위에 있습니다. 다른 구성을 고르면 같은 질문에 다른 답이 나오므로 폴더가 갈립니다.

## 1. 이 구성을 고르는 조건

분리 자체가 목적이 아닙니다. 분리는 비용을 치르고 사는 것이며, 그 비용이 이득을 넘기는 구간이 실재합니다.

| 조건 | 판단 |
| :--- | :--- |
| 클라이언트가 둘 이상 (웹 · 모바일 앱 · 외부 연동) | 분리합니다. 표현을 서버가 소유하면 클라이언트마다 서버가 갈라집니다 |
| 프론트엔드와 백엔드의 담당이 나뉘어 있음 | 분리합니다. 계약이 협업의 접점이 됩니다 |
| 화면의 상호작용이 서버 왕복으로 표현되지 않음 | 분리합니다 |
| 위 셋 중 어느 것도 아님 | **분리하지 않는 편이 낫습니다** |

마지막 줄이 가장 자주 잊힙니다. 관성으로 분리를 고르면 아래 네 가지가 얻는 것 없이 순수 비용으로 남습니다.

## 2. 치르는 비용

| 비용 | 내용 |
| :--- | :--- |
| **계약** | 두 저장소가 REST 계약 하나로만 만나므로 그 계약을 문서로 유지해야 합니다 |
| **인증 왕복** | 토큰 보관과 갱신을 클라이언트가 다룹니다. 세션 하나로 끝나지 않습니다 |
| **출처 분리** | 자산과 API의 출처가 다르면 CORS 설정이 배포 구성의 일부가 됩니다 |
| **배포 두 벌** | 파이프라인과 롤백 절차가 각각 필요하며 배포 순서를 정해야 합니다 |
| **검증 두 벌** | 같은 규칙이 양쪽에 존재합니다. 이 중복은 제거 대상이 아닙니다 |

마지막 항목만 성격이 다릅니다. 앞의 넷은 없앨 수 있으면 없애는 편이 나은 비용이지만, 검증 중복은 **없애면 안 되는 비용**입니다. 프론트엔드 검증을 생략하면 사용자가 제출 후에야 오류를 알게 되고, 서버 검증을 생략하면 데이터가 손상됩니다.

## 3. 문서의 구성

| 문서 | 답하는 질문 |
| :--- | :--- |
| [시스템 아키텍처](system/index.md) | 무엇이 어디에 배포되고 바깥과 어떻게 만나는가 |
| [애플리케이션 아키텍처](application/index.md) | 앱을 몇 개로 나누고 각자 무엇을 소유하는가 |
| 스택별 문서 | 그 경계 안쪽의 코드를 어떻게 조직하는가 |

세 층은 위에서 아래로 좁혀집니다. **위 층의 결정이 아래 층의 전제가 되며 반대 방향은 없습니다.** 스택 문서의 제약 조건 절에 위 층의 결정이 인용되는 것은 그 때문입니다.
